version: '3.8'

services:
  config-client:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: config-client-container
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    ports:
      - "8080:8080"
    networks:
      - my-network

networks:
  my-network:
    external: true
